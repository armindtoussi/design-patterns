package bridge.pattern;

/**
 * Draw api, interface responsible for changing how shapes are 
 * drawn. 
 * 
 * @author armin2
 *
 */
public interface DrawAPI {
	public void drawCircle(int radius, int x, int y);
}
